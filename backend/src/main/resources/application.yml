spring:
  application:
    name: replate-backend

  data:
    mongodb:
      uri: mongodb://localhost:27018/replate

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info